# Copy to configs/ddns.env on Widow (chmod 600). Do not commit ddns.env.
# DuckDNS: https://www.duckdns.org/ — subdomain only (not the full hostname).
#
# DUCKDNS_DOMAIN=mydemo     → updates mydemo.duckdns.org
# DUCKDNS_TOKEN=            → from DuckDNS dashboard

DUCKDNS_DOMAIN=
DUCKDNS_TOKEN=
