# Root analysis snapshots

The `analysis/` folder that lived at the repository root (CSV/JSONL/Markdown operational dumps) was **not** part of the runtime pipeline. Moved here for housekeeping.

Safe to delete locally if you do not need historical stats.
