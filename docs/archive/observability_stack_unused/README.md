# Observability stack (unused in default bare-metal path)

Former repo-root `monitoring/` (`prometheus.yml`, Grafana assets). Not wired into `start_system.sh`. Archive only; use host Prometheus/Grafana if you need metrics.
