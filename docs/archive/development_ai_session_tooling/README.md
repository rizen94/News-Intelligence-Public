# Development AI session tooling (archived)

The former `development/scripts/` tree (AI session start/end/promote/rollback, dev restarts) is **optional helper shell** for Cursor-style workflows. It is **not** invoked by `start_system.sh`, CI, or production.

Restore pieces to `development/scripts/` at repo root only if you actively use them.
