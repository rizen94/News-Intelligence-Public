You are working on the News Intelligence System v3.0.  

Your next tasks are to **standardize the backend API on FastAPI** and **improve the frontend UI/UX** so users can take full advantage of the system’s capabilities.

### 1. Backend: Standardize on FastAPI
- Remove Flask dependencies and routes — migrate all endpoints to FastAPI.
- Ensure all API modules (`collectors`, `intelligence`, `monitoring`, `prioritization`) expose FastAPI routes.
- Enable auto-generated OpenAPI/Swagger docs:
  - Available at `/docs` (Swagger UI) and `/redoc`.
  - Ensure all endpoints use proper request/response models (Pydantic).
- Add middleware for:
  - Logging & request tracing
  - Prometheus metrics (integration with monitoring stack)
- Update `requirements.txt` to remove Flask and include FastAPI, Uvicorn, and Pydantic.

### 2. Frontend: UI/UX Redesign
- Review the current React UI (`web/src/`) and align it with the full API capabilities.
- Key improvements:
  - **Dashboard**: Show real-time ingestion stats, ML pipeline status, and alerts.
  - **Articles**: Improve filtering/search, add sentiment/entity visualization.
  - **Story Dossiers**: Timeline visualization of story evolution.
  - **Monitoring Page**: Include application-level metrics (articles/hour, processing latency).
  - **Search Page**: Integrate RAG-enhanced search with clear results highlighting.
- Add new components or improve existing ones in `components/` to support these features.
- Ensure the UI consumes the updated FastAPI endpoints through `services/newsSystemService.js`.

### 3. Deliverables
- Fully FastAPI-based backend (no Flask).
- Updated React frontend with intuitive navigation and visualizations.
- Updated documentation:
  - API reference (auto-generated).
  - UI guide showing how to access new features.
- Ensure all changes work in Docker Compose deployment.

Focus on **consistency, usability, and clarity** — the backend should be clean and standardized, and the frontend should make it easy for users to explore the system’s intelligence features.