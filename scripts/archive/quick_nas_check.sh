#!/bin/bash
# Quick NAS Container Check - Copy/paste these commands after SSH connection

echo "📋 Commands to run on NAS after SSH connection:"
echo "================================================"
echo ""
echo "1. Check all running containers:"
echo "   docker ps"
echo ""
echo "2. Check PostgreSQL containers (all states):"
echo "   docker ps -a --filter 'name=postgres' --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}\t{{.Image}}'"
echo ""
echo "3. Check if PostgreSQL is running:"
echo "   docker ps --filter 'name=postgres'"
echo ""
echo "4. If PostgreSQL container exists, test it:"
echo "   CONTAINER=\$(docker ps --filter 'name=postgres' --format '{{.Names}}' | head -1)"
echo "   if [ -n \"\$CONTAINER\" ]; then"
echo "     echo \"Testing container: \$CONTAINER\""
echo "     docker exec \$CONTAINER pg_isready -U postgres"
echo "     docker exec \$CONTAINER psql -U postgres -c 'SELECT version();' | head -2"
echo "   fi"
echo ""
echo "5. Check PostgreSQL logs (if container exists):"
echo "   docker ps -a --filter 'name=postgres' --format '{{.Names}}' | head -1 | xargs -I {} docker logs --tail 20 {}"
echo ""
echo "6. List all containers:"
echo "   docker ps -a --format 'table {{.Names}}\t{{.Status}}\t{{.Image}}'"
echo ""
echo "================================================"
echo "To connect: ssh -p 9222 Admin@192.168.93.100"
echo "Password: <NAS_PASSWORD_PLACEHOLDER>"

